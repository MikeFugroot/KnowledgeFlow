# KnowledgeFlow Web 版 — Python 依赖
# 版本策略：核心框架锁定主版本，ML 包放宽以保证兼容性

# ---- Web 框架 ----
fastapi>=0.111
uvicorn[standard]>=0.30

# ---- 数据库 ----
sqlalchemy>=2.0
alembic>=1.13
aiosqlite>=0.20
greenlet>=3.0       # SQLAlchemy 异步引擎依赖

# ---- 数据校验 ----
pydantic>=2.7
pydantic-settings>=2.3

# ---- 文件解析 ----
PyMuPDF>=1.24
python-docx>=1.1

# ---- 视频转写 ----
# 注意：torch 建议先通过 conda 安装（见 SETUP-GUIDE.md），再装其他包
faster-whisper>=1.0
openai-whisper>=20231117

# ---- 语义检索 ----
sentence-transformers>=3.0
faiss-cpu>=1.8
torch>=2.3      # 如果已通过 conda 装了 torch，pip 会自动跳过
numpy>=1.26

# ---- 网页抓取 ----
trafilatura>=1.8
beautifulsoup4>=4.12
requests>=2.32
yt-dlp>=2024.5

# ---- 异步任务 ----
arq>=0.26

# ---- 安全 ----
cryptography>=43.0
python-multipart>=0.0.9

# ---- 工具 ----
httpx>=0.27
python-jose>=3.3
